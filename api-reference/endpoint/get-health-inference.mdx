---
openapi: "GET /health/inference"
---

Connectivity check for the inference service used by workers.

This endpoint is intended for operations checks and is available without JWT.

## Example

```bash
curl -X GET "http://localhost:8000/health/inference"
```

{
  "engines": {
    "node": ">=18 <25"
  },
  "scripts": {
    "preinstall": "node -e \"const [major]=process.versions.node.split('.').map(Number); if (major < 18 || major >= 25) { console.error('Unsupported Node version: ' + process.versions.node + '. Use Node 24.x or an LTS release.'); process.exit(1); }\"",
    "dev": "mintlify dev --no-open",
    "dev:quick": "mintlify dev --no-open --disable-openapi"
  },
  "dependencies": {
    "mintlify": "^4.2.536"
  }
}
